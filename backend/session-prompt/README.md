# session-prompt

Derived prompt-context builders live here.

- `label-context.mjs`: builds naming / labeling prompt context from session metadata, runtime state, and lightweight memory sources.

This directory is prompt-side derived context, not session truth.
