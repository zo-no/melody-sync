# Hooks Settings Frontend Map

This directory owns the hooks settings panel.

- `model.js`: lifecycle phase definitions, event fallback metadata, phase grouping helpers, and the lightweight lifecycle flow model for the settings panel.
- `ui.js`: browser entry and DOM rendering for the hooks settings overlay.
