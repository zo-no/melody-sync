# CLAUDE.md — Compatibility Shim

> This repo's canonical AI context now lives in `AGENTS.md`.
> If your tool auto-loads `CLAUDE.md`, immediately open `AGENTS.md` and treat it as the source of truth.
> Keep ongoing project context, architecture notes, and `Current Priorities` updates in `AGENTS.md`, not here.
